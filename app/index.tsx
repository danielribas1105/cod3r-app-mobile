import Pagina from "@/components/templates/pagina";
import { Text } from "react-native"

export default function Index() {
  return (
      <Pagina>
        <Text style={{fontFamily: 'Space-Mission'}}>
          New app React-native
        </Text>
      </Pagina>
  );
}
